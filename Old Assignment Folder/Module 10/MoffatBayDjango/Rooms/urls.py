from django.urls import path
# from .views import RoomListView, RoomDetailView

# Group Names: Taylor Mommer, John Garcia, Andrew Bach, Somsak Bounchareune, Torren Davis

# This file contains the URL patterns for the Rooms app.
urlpatterns = [
    # path('', RoomListView.as_view(), name='room_list'),
    # path('<int:pk>/', RoomDetailView.as_view(), name='room_detail'),
]
